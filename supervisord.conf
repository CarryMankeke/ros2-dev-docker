# Pseudocódigo de supervisord para orquestar componentes gráficos

"""pseudo
supervisord (nodaemon=true):
  programa xvfb:
    usuario: root
    comando: Xvfb :0 -screen 0 1920x1080x24 -ac +extension GLX +render -noreset
    entorno: DISPLAY=:0
    prioridad: 5
    auto: start + restart
    logs: /var/log/xvfb.log

  programa xfce:
    usuario: root
    comando: preparar /tmp/runtime-root ; exportar XDG_RUNTIME_DIR ; startxfce4
    entorno: DISPLAY=:0
    prioridad: 10
    auto: start + restart
    logs: /var/log/xfce.log

  programa x11vnc:
    usuario: root
    comando: x11vnc -forever -nopw -display :0 -rfbport 5900 -listen 0.0.0.0 -xkb -shared
    prioridad: 15
    auto: start + restart
    logs: /var/log/x11vnc.log

  programa novnc:
    usuario: root
    comando: websockify --web=/usr/share/novnc 8080 localhost:5900
    prioridad: 20
    auto: start + restart
    logs: /var/log/novnc.log
"""
