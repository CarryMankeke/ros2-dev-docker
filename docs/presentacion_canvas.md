# Presentación en pseudocódigo (Canvas)

"""pseudo
objetivo: Armar slides para la simulación de robot en ROS 2 + Gazebo + RViz.
formato:
  - tipografía uniforme
  - colores claros con acentos
  - numerar páginas y figuras
  - usar capturas nativas en alta resolución

estructura_slides():
  1. portada(titulo, autor, fecha, logo)
  2. indice(lista_secciones)
  3. introduccion_objetivo(herramientas=[ROS2 Jazzy, Gazebo, RViz, micro-ROS, ros2_control])
  4. diseno_robot(componentes, frames, figura_urdf)
  5. sensores_plugins(topicos, frecuencia, figura_sensor)
  6. control_ros2_control(controladores, teleop, diagrama_flujo)
  7. integracion_micro_ros(nodo/firmware, topicos, conectividad)
  8. visualizacion_rviz(robot_model, TF, joint_state_publisher, figura_rviz)
  9. simulacion_gazebo(mundo, spawn, sensores, figura_gazebo)
 10. launchfiles_y_flujo(comando_ros2_launch, diagrama_arranque)
 11. resultados_pruebas(movimientos, tabla_pruebas, captura_movimiento)
 12. conclusiones_y_futuro(logros, limitaciones, mejoras)
 13. referencias(formato=ISO690|IEEE)

notas_canvas():
  - una página por slide siguiendo el orden
  - captions numeradas ("Fig. 2 - Vista de LIDAR en Gazebo")
  - contraste legible y alineación consistente
  - exportar manteniendo resolución original de capturas
"""
